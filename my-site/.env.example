# Скопируйте в .env.development / .env.production при необходимости.

# Dev (по умолчанию): относительный путь + Vite proxy → http://localhost:5183
VITE_API_BASE_URL=/api

# Prod on the same custom domain: relative API route
# VITE_API_BASE_URL=/api
