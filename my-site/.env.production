# Production uses the custom domain's /api route, which DigitalOcean forwards
# to the backend service in the same app.
VITE_API_BASE_URL=/api
# Default tenant for the shared/root public URL. Tenant subdomains still win
# in the backend, so this does not override the carwash hostname.
VITE_ORGANIZATION_ID=thetochka
